{
  "build": {
    "builder": "NIXPACKS",
    "nixPkgs": ["python311", "nodejs_20", "gcc"]
  },
  "deploy": {
    "startCommand": "pip install -r requirements.txt && gunicorn -w 1 --threads 8 -b 0.0.0.0:$PORT app:app"
  }
}
